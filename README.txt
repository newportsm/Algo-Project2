To run script -

python project2.py <input file name (no extension)>

With the <input file name> do not include the extension.
Example. Coin1.txt would be entered as Coin1

Be aware that the changeslow algorithm takes a VERY long time after total value > 30, recommended to only use small values to test changeslow.